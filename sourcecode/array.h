int ** create(int lineNumber, int columnNumber);// a function which take as parameter the number of line and column you want and return the pointer to a two-dimensional array//
int randomize(int **array);//a function which takes a two dimensional array as parameter and return a shuffled array//
